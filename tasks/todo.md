# Dashboard UI Run Todo List

- [x] Start the FastAPI backend server using `.venv/bin/researchpulse serve`
- [x] Run `npm install` in the `frontend` directory
- [x] Start the Next.js dev server using `npm run dev` in the `frontend` directory
- [x] Verify that the dashboard UI loads and is functional at `http://localhost:3000`

## Review Section

All verification tasks have been successfully completed:
1. Started the FastAPI backend server on port 8001 (due to port 8000 being used by another process).
2. Installed npm packages and started the Next.js development server on port 3000.
3. Successfully verified and captured a screenshot of the dashboard rendering the 40 scraped news items with no errors.
